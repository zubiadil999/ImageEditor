# Laravel Image Editor

This web-based image editor allows users to perform basic image editing tasks such as Cleanup (removing unwanted objects) or Remove Background using the ClipDrop API.

## Setup

1. **Clone the repository:**

   ```bash
1.  git clone https://github.com/your-username/your-repository.git
2.  cd your-repository
4.  composer install
5.  cp .env.example .env
6.  php artisan key:generate
7.  php artisan migrate
8.  php artisan serve

## Testing

2. **To run tests, use the following command:**

1.  php artisan test
2.  heroku create
3.  heroku config:set CLIPDROP_API_KEY=your-clipdrop-api-key
4.  git push heroku master
5.  heroku run php artisan migrate
6.  heroku open
